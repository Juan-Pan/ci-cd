name: CD - Despliegue a Render

# ¿CUÁNDO SE EJECUTA?
on:
  push:
    branches: [ "main" ] # SOLO cuando el código llega a la rama principal

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Avisar a Render del despliegue
        # Render usa una URL secreta (Deploy Hook) para saber cuándo actualizarse.
        # Más adelante configurarás esto en los 'Secrets' de GitHub.
        run: |
          echo "Simulando despliegue a Render..."
          # Cuando tengas la URL real, descomenta la línea de abajo:
          # curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
